# Environment Configuration Template
# Copy this file to .env and fill in your values

# Encryption Key (base64-encoded Fernet key)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# IMPORTANT: Keep this secret! Losing this key means losing access to encrypted credentials.
ENCRYPTION_KEY=

# Optional: Override default database path
# DATABASE_URL=sqlite:///./backend/arena_cin7.db
